import Screen from '@components/UI/Screen';
import React from 'react';

type Props = {};

const SetPasswordScreen: React.FC<Props> = ({}) => {
  return <Screen safeAreaEdge="all" />;
};

export default SetPasswordScreen;
